# 规格书：GitHub 驱动的开源应用市场

> Triage: ready-for-agent
> 创建时间：2026-08-22
> 状态：草案 v0.1（待确认项见 Further Notes）
> 项目跟踪器：未配置远程跟踪器，本规格以文件形式发布在工作区 docs/ 目录

## Problem Statement

目前缺少一个以 GitHub 为唯一分发源头、全自动化运营的 Android 开源应用市场。开发者发布应用需要依赖第三方商店后台、人工审核或自建分发系统，来源不可追溯、元数据易过期、门槛高；应用商店维护者需要投入大量人工进行收录、信息整理与质量把关；用户难以快速判断应用的来源可信度、开源状态、所需特殊权限（ADB / Shizuku / Root 等）与质量等级。

本系统要解决的核心问题：让应用收录、信息采集、评级、分发全流程自动化，同时保证来源可追溯、元数据零人工维护、质量分等清晰。

## Solution

建立"承载仓库 + 三个 CICD 工作流 + 客户端应用"三位一体的应用市场系统：

- 承载仓库（GitHub 公开仓库）是唯一分发源头，只存放每个应用的元数据（每个应用一个文件夹、两个 JSON 文件），APK 二进制始终留在开发者自己的 GitHub Release 中，仓库不托管二进制。
- 开发者通过 Fork 承载仓库、在应用文件夹中编写第一个 JSON（用户文件，包含仓库地址、是否开源、最低特殊权限），提交 PR 即完成收录申请。
- 工作流 1 在 PR 创建时校验改动范围：只允许新增或修改第一个 JSON，禁止创建或修改第二个 JSON（自动生成文件），违规即关闭 PR 并留言。
- 工作流 2 核验应用仓库：分析"真开源/假开源"；若声明开源则检查仓库是否发布过 Release、Release 中是否有 APK 文件；任一步失败即关闭 PR、写明关闭理由并留言。
- 核验通过后采集应用信息：开源应用直接从仓库拉取图标、应用名称、权限列表；非开源应用从最新 Release 下载 APK 并解包，提取图标、名称、权限。全部信息在合并 PR 时由 CICD 自动写入第二个 JSON。
- 每个收录应用被评级为 A / B / C / D / E 五个等级：算法只依据 Star 数评定 E/D/C 级，C 级以上（B/A）与闭源抬升由人工完成。
- 工作流 3 每月初统计社区推荐票（ISSUE），票数前 2~3 名的应用自动 @ 维护者进入人工评级审核。
- 客户端应用（本工作区 Android 项目）读取承载仓库，向用户提供应用浏览、等级筛选、详情展示与 APK 下载分发。

## User Stories

1. 作为开发者，我想通过 Fork 承载仓库并提交 PR 来发布我的 Android 应用，以便不需要任何后台系统和人工审批。
2. 作为开发者，我想只填写一个轻量 JSON（仓库地址、是否开源、最低特殊权限）即可完成收录申请，以便发布成本最低。
3. 作为开发者，我不需要自己准备图标、名称、权限等元数据，以便减少重复劳动并保证信息一致。
4. 作为开发者，当我误改或创建了自动生成的第二个 JSON 时，我想收到带明确原因的 PR 关闭留言，以便修正后重新提交。
5. 作为开发者，当我的声明与实际不符（假开源、无 Release、Release 无 APK）时，我想立即得知具体失败点，以便补齐条件后再来。
6. 作为维护者，我想在 PR 创建时自动校验改动范围，只允许动第一个 JSON，以便保护自动生成文件不被篡改。
7. 作为维护者，我想自动核验"真开源/假开源"，以便将空壳仓库、假开源仓库挡在门外。
8. 作为维护者，我想在合并 PR 时自动生成第二个 JSON（图标、名称、权限等），以便商店数据零人工维护。
9. 作为维护者，我想对每个收录应用自动评级（A-E 五级），以便应用质量一目了然。
10. 作为用户，我想按等级筛选和排序应用，以便快速找到高质量可信应用。
11. 作为用户，我想在应用详情中看到开源状态与最低特殊权限（无 / ADB / Shizuku / Root），以便判断应用是否适合我的设备。
12. 作为用户，我想直接下载来自开发者 GitHub Release 的 APK，以便来源可追溯、可核验。
13. 作为用户，我想看到 APK 校验和与来源 Release 信息，以便确认文件完整性与真实性。
14. 作为客户端，我想从承载仓库拉取全部应用索引并展示列表、详情、下载，以便提供完整的应用市场体验。
15. 作为维护者，我想工作流可安全重跑且幂等，以便偶发失败不会产生脏数据或重复收录。
16. 作为开发者，我想通过同样的 PR 流程更新已有应用（指向新的 Release），以便所有变更都有记录可追溯。
17. 作为用户，我想查看评级结果，以便理解评级结果并建立信任。
18. 作为用户，我想每月提交一次推荐票（ISSUE），提名我认为值得晋升的开源应用，以便社区参与评级。
19. 作为维护者，我想每月自动收到推荐票数前 2~3 名的应用审核通知，以便社区驱动的评级晋升零人工统计。

## Implementation Decisions

### 系统组成与职责边界

系统由三部分组成。承载仓库负责收录元数据并作为分发索引；CICD 工作流负责校验、核验、采集、评级与自动文件生成；客户端应用负责面向用户的浏览与下载。三者通过明确的文件约定和 GitHub API 契约衔接，互不耦合。

### 承载仓库布局约定

每个应用独占一个文件夹，文件夹内恰好包含两个 JSON 文件。第一个 JSON 由开发者编写并随 PR 提交，字段由人维护；第二个 JSON 由 CICD 在合并 PR 时自动生成，人工不得触碰。承载仓库不存放 APK 二进制，APK 始终位于开发者仓库的 Release 资产中，第二个 JSON 只记录其下载地址与校验信息。

### 第一个 JSON（用户文件）字段

包含三个核心字段：仓库地址（开发者应用的 GitHub 仓库位置）、是否开源（布尔声明值，将由工作流独立核验）、最低特殊权限（应用运行所需的最低特殊权限，取值来自 none / adb / shizuku / root 枚举，可多选，缺省为 none）。该文件是工作流 1 与工作流 2 的唯一人工输入。

### 第二个 JSON（自动生成文件）字段

包含：应用包名；显示信息（应用名称、图标引用）；权限列表（应用声明的全部权限，来自开源仓库清单或 APK 解包结果）；版本信息（versionName、versionCode、来源 Release tag）；来源信息（仓库 owner/repo、开源核验结论、APK 下载地址、APK SHA-256 校验和）；评级（grade 的当前值，可能被人工调整覆盖）；生成信息（生成时间戳、生成工作流标识）。该文件是客户端展示与下载的权威数据源。

### 工作流 1：PR 文件范围校验

触发于 PR 创建或同步。规则：本次改动仅允许新增或修改各应用文件夹下的第一个 JSON 文件；一旦检测到创建或修改第二个 JSON 文件即视为违规。违规处理：关闭该 PR，并在 PR 上留言，明确指出违规文件与违规原因。

### 工作流 2：应用核验与信息采集

输入为 PR 中的第一个 JSON。核验链依次为：应用仓库存在且可访问；若声明开源则核验"真开源"（依据规则集：存在有效 LICENSE、源码真实非占位、仓库未被归档等）；检查最新 Release 存在且其资产中包含 APK 文件。核验链任一步失败：关闭 PR、写明关闭理由并留言告知。核验通过后进入信息采集：开源应用从仓库侧获取图标、名称、权限列表；非开源应用从最新 Release 下载 APK，解包提取图标、名称、权限。随后实时抓取 Star 数完成算法评级（E/D/C），采集结果与评级在合并 PR 时落盘为第二个 JSON 并提交入库。

### 应用评级 A-E（定稿）

评级由"算法初评 + 人工调整"两层构成，等级定义：E 级为普通闭源应用；D 级为普通开源应用与较优质闭源应用；C 级为社区认可度高的开源应用（原则上不允许闭源进入，特别优质的闭源可由维护者破例给 C）；B 级为某一方面表现突出的开源应用（不允许闭源进入）；A 级为各个方面均表现突出的开源应用。

算法初评规则（自动、合并 PR 生成快照时实时抓取 Star 数）：开源应用 Star 数达到阈值（10k，可配置）评定 C 级，未达到评定 D 级；闭源应用一律评定 E 级，算法无权干涉闭源评级。算法唯一变量是 Star 数，不存在存疑状态，抓取失败即重新计算。

人工调整规则：C 级以上（B/A）全人工评级，社区推荐票是唯一触发通道——每个账号每月可提交一次推荐票（以 ISSUE 形式，固定模板加专属标签，编辑不改投，重复提交作废），工作流 3 每月初统计票数，前 2~3 名自动 @ 维护者进入审核；入选只代表获得审核资格，最终 B 或 A 由维护者逐个判断。闭源应用不参与推荐票，其晋升（D 级，特别优质可破例 C 级）由维护者主动调整。维护者可直接下调任何应用的评级，无需记录与公示。

评级变更路径仅两条：合并 PR 时随第二个 JSON 快照重算，以及维护者的人工调整；不设定时全量重扫。评级角色为展示标签兼列表排序/筛选权重，不设硬性准入门槛（准入由工作流 2 核验链把关）。

### 工作流 3：月度推荐票计票

每月初触发。统计上月携带推荐票标签的 ISSUE，按应用聚合票数，生成票数前 2~3 名的应用名单并 @ 维护者进入人工评级审核。计票规则与防刷规则见人工调整规则。

### 客户端应用（本工作区 Android 项目）

客户端采用数据层与 UI 层分离的结构。数据层通过 GitHub API 读取承载仓库目录与两个 JSON、解析应用索引，并承担 APK 下载链路；UI 层基于 Compose 提供列表、详情、等级筛选与下载交互。数据层不依赖 UI，可独立测试。APK 下载链路可参考用户已有 GitLink 项目的分块加速与镜像经验。

### 健壮性要求

工作流全部幂等且可安全重跑；对 GitHub API 限流、Release 资产缺失、APK 损坏、JSON 校验失败等异常路径给出明确、可读的失败信息；关闭 PR 的留言必须包含可操作的修正指引。

## Testing Decisions

主测试接缝放在"校验与采集逻辑"上：将工作流 1 的文件范围校验、工作流 2 的开源核验与信息采集、评级算法（Star 阈值判定）与工作流 3 的计票逻辑实现为独立 CLI 工具，GitHub Actions 只做薄封装调用。这样可以用本地样例仓库与样例 JSON 直接测试 CLI 行为，无需真跑 CI。客户端侧接缝放在数据层：Repository 对 GitHub API 与 JSON 解析的测试用 JVM/Robolectric 完成，Compose UI 用 UI 测试覆盖。

好测试的判据：只断言外部行为，即给定输入（JSON 内容、仓库状态、Release 资产、Star 数、推荐票 ISSUE 集合）后断言输出（校验结论、关闭动作与留言内容、生成的第二个 JSON 字段、评级结果、月度计票名单），不测内部实现细节。

现有先例：工作区模板已自带 app/src/test 与 androidTest 结构，可直接沿用；CLI 逻辑采用脚本自测加样例 fixture 的通用做法。

## Out of Scope

客户端的最终 UI 设计与视觉细节；支付、评论、账号、推送等商店增值功能；非 Android 平台应用收录；在承载仓库内托管 APK 二进制；评级的定时全量重扫、公开申诉与争议流程（人工调整不记录不公示，开发者无申诉通道）；PR 自动合并的决策（合并动作由人工或维护策略决定）；承载仓库与 CICD 的具体文件命名与实现细节。

## Further Notes

待确认项清单：两个 JSON 的文件名约定；图标在承载仓库内的存放与引用方式（独立资产目录还是 JSON 内 URL）；开源应用图标、名称、权限的拉取来源约定（仓库内指定路径、README 还是源码清单）；是否需要自动合并 PR。评级框架已定稿（见实现决策章节），评级相关事项不再列为待确认。

定稿说明：应用评级 A-E 采用"算法初评 + 人工调整"两层结构，算法唯一变量为 Star 数（开源 ≥10k 进 C、<10k 落 D，闭源一律 E），B/A 级由社区推荐票触发人工审核，闭源晋升由维护者主动调整，维护者可直接降级且不记录；评级仅随 PR 合并快照与人工调整两条路径变更，无定时重扫；版本信息只在重提 PR 时刷新，开发者仓库后续发布的新 Release 不会自动同步进商店。

术语约定：承载仓库（分发索引仓库）、第一个 JSON（用户文件）、第二个 JSON（自动生成文件）、工作流 1（文件范围校验）、工作流 2（核验与采集）、工作流 3（月度推荐票计票）、真开源（经规则集核验通过的开源声明）。